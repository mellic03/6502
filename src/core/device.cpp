#include <memu/device.hpp>
#include <cassert>

// HwDevice::HwDevice( DataBus *bus )
// :   mBus(bus)
// {
//     if (mBus)
//     {
//         mBus->attach(this);
//     }
// }


// ubyte HwDevice::busRead( uint16_t i )
// {
//     if (!mBus) return 0;
//     return mBus->read(i);
// }

// void HwDevice::busWrite( uint16_t i, ubyte v )
// {
//     if (!mBus) return;
//     mBus->write(i, v);
// }

